## Books 25 & 26 — Deepen in place (full per-door depth)

Same template + discipline as Book 22. **No door-count changes** (B25=8, B26=10), so no engine/XP/rank/SAVE_KEY/README-total changes — purely per-door enrichment + one-time diagram-CSS port per book.

### Shared gap (both books, identical to Book 22 pre-expansion)
- 0 SVG diagrams; diagram CSS subsystem (`.diagram`/`.diag-title`/`.diag-cap`/`.cell`/`.cell-hot`/`.cell-good`/`.lbl-sm`) MISSING from both `style.css`.
- 0 code-blocks; B26 has 0 compare, B25 has 2/8; B25 has 0 stat-grids, B26 has 1/10.
- `senior` insight already rendered by both engines + present on most doors — keep/enrich.
- Both books define the SAME CSS color vars as Book 22, so the diagram CSS port is a verbatim paste that auto-themes.

### Per-door enrichment template (every door keeps its mentor/scene/spirit/recall/secret/senior; INSERT only):
1. One SVG diagram (viewBox + defs markers + lbl-sm/cell-* helpers)
2. One code-block (concept distilled as mnemonic/checklist)
3. One stat-grid (key numbers: forgetting %, hour milestones, replication stats)
4. A compare box (myth vs truth) if the door lacks one
5. Preserve all existing .timeline/.verse/.callout/.kv-table content

### Files
- Book 25 `deep-learning/`: style.css (diagram CSS), doors-1-4.js (D1-4), doors-5-8.js (D5-8). Engine untouched.
- Book 26 `knowledge-craftsman/`: style.css (diagram CSS), doors-1-5.js (D1-5), doors-6-10.js (D6-10). Engine untouched.
- Root: index.html both book-card game-desc (note new depth); README.md minor row touch-ups (NO totals change).

### Sequence (Book 25 fully, then 26), commit every 2 doors
1. B25 setup (diagram CSS port) → verify → commit
2. B25 D1-D2, D3-D4, D5-D6, D7-D8 (4 commits, each verified)
3. B25 finalize (root card + README) → commit
4. B26 setup → commit
5. B26 D1-D2, D3-D4, D5-D6, D7-D8, D9-D10 (5 commits)
6. B26 finalize → commit

~12 commits total.

### Verification each step (same as Book 22)
- node -c per file + concatenation bundle
- Runtime load test (concat → eval) confirming each door has svg+code-block+compare+senior+recall — this caught backtick + .split() bugs in Book 22
- Live http.server smoke test per book

### Risks (same mitigations as Book 22)
- Backticks inside .story template literals → avoid; runtime test catches
- SVG marker id collisions → unique per door
- Preserve each book's narrative thread (B25 D8 references all 7 prior elders; B26 has Ebbinghaus→Adler→Zettelkasten progression)